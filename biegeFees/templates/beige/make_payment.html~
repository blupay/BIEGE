{% extends "beige/dashboard.html" %}

{%block title%}<title>Payment| Make Payments</title> {%endblock title%}


{% block content %}

<div class="panel panel-danger">
  <!-- Default panel contents -->
  <div class="panel-heading">New Transaction</div>
  <div class="panel-body">
  <h4> <font color= "grey">Account Name : </font> {{student_.fullnameC}}</h4>
  <h4> <font color= "grey">System ID : </font> {{student_.studentID}}</h4>
   <h4> <font color= "grey">School : </font> {{student_.schoolID.schoolName}}</h4>
  
 <form class="form-horizontal" role="form" action="." method="post">
   
   <div class="form-group">
    <label for="inputEmail3" class="col-sm-2 control-label">Fees Type</label>
    <div class="col-sm-6">
   <select class="form-control" name="fee">
   <option value="0" selected="selected" disabled>-Select-</option>
   {% for c in fees %}
 <option value="{{c.name}}">{{c.name}} </option>
{% endfor %}  
 
   </select>
    </div>
  </div>
  <div class="form-group">
  {{amount_error}}
    <label for="inputEmail3" class="col-sm-2 control-label">Amount</label>
    <div class="col-sm-6"> {% ifequal amount_error '' %} <font color="red">field required *</font> {% endifequal %}
      <input type="number" step="any" name="amt" class="form-control" id="inputEmail3" placeholder="Amount">
    </div>
  </div>
  <div class="form-horizontal">
    {% for field in form %}
    {{field.label_tag}}
    <font color="red">{{field.errors}}</font>
    {{field}} </br> </br>
    
    {% endfor %}
  </div>

<button class="btn btn-lg btn-primary btn-block" name="preConfirm" type="submit" value="preConfirm" style="width:150px;">Submit</button>
</form>


</div>
</div>
{{popup}}

{% ifnotequal popup '' %}

{{popup}}
<div class="modal fade" id="myModal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title">Confirm Payment</h4>
      </div>
      <div class="modal-body">
        <p>Confirm Fees for <br>
        ID : {{student_.studentID}}<br>
        Fullname : {{student_.fullnameC}}<br>
        Fee Type : {{getFee}}<br>            
        Amount :  <h1 style="color:red;">GHS {{getAmt}}</h1>
        
        
        
        
        
        </p>
      </div>
      <div class="modal-footer">
      <form class="form-horizontal" role="form" action="." method="post">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="submit" name="Confirm" value ="Confirm" class="btn btn-primary">Save changes</button>
      </form>
      </div>
    </div><!-- /.modal-content -->
  </div><!-- /.modal-dialog -->
</div><!-- /.modal -->

     {% endifnotequal %}

{% endblock %}
